# CoreMinds: education funding pages (proposed change)

A proposed change to the CoreMinds website, [coremindstutor.org](https://coremindstutor.org), opened here for review. **Don't merge it here.** This repository isn't connected to the website.

The site's own repository is private, so this repository holds only the files the change touches:

- `main` has the four existing files the change edits, as of commit `515f864` (July 2, 2026). That is the newest version I have. The live site has changed since.
- The pull request adds three new files and edits those four.

To apply the change to the real repository, add `.patch` to the end of the pull request's URL to download it, then run `git am -3` on it in a copy of that repository. The pull request has the details.
